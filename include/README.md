# Public Include Directory

This template keeps application-local headers under `src/` so the ESP-IDF
component boundaries remain explicit while the project is still small.

Use this directory only for headers that must be consumed by external
components or examples.
