# Optional USB / TinyUSB

TinyUSB is prepared as future work:

```sh
pio run -e cardputer_adv_usb
```

Use this for CDC, MIDI or HID experiments only after the base flashing and
serial logging workflow is stable.
